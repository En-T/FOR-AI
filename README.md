# School Management System - Frontend

Полный фронтенд для веб-приложения управления школой с единым стилем и современным дизайном.

## 📁 Структура проекта

```
static/
├── css/
│   ├── colors.css          # Палитра цветов и CSS переменные
│   ├── main.css           # Базовые стили и утилиты
│   ├── layout.css         # Navbar, sidebar, grid layouts
│   ├── buttons.css        # Стили для всех вариантов кнопок
│   ├── forms.css          # Стили форм и валидация
│   ├── tables.css         # Интерактивные таблицы
│   └── responsive.css     # Адаптивные стили
└── js/
    ├── main.js            # Главный скрипт, AJAX, инициализация
    ├── utils.js           # Вспомогательные функции
    ├── forms.js           # Валидация форм
    ├── tables.js          # Работа с таблицами
    ├── modals.js          # Модальные окна
    └── transfer-box.js    # Компонент распределения студентов

templates/
├── base.html              # Базовый шаблон с navbar/sidebar
├── login.html             # Страница входа
├── index.html             # Главная страница (ролевая)
├── 404.html               # Страница ошибки 404
└── 500.html               # Страница ошибки 500
```

## 🎨 Дизайн-система

### Цветовая палитра
- **Первичный**: `#2563eb` (синий)
- **Вторичный**: `#1e40af` (темно-синий)
- **Успех**: `#16a34a` (зеленый)
- **Ошибка**: `#dc2626` (красный)
- **Предупреждение**: `#f59e0b` (оранжевый)
- **Фон**: `#f9fafb` (светло-серый)
- **Текст**: `#1f2937` (темно-серый)

### Типографика
- **Основной шрифт**: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
- **Заголовки**: Poppins (600-700 вес)
- **Размер текста**: 14px (основной)

### Спейсинг
Базовая единица: 8px
- 8px, 16px, 24px, 32px, 48px, 64px

### Эффекты
- **Тени**: Консервативные box-shadow для глубины
- **Радиус**: 4px (кнопки/inputs), 8px (карточки)
- **Градиенты**: На кнопках и важных элементах

## 🎯 Особенности реализации

### CSS
- **Без фреймворков**: Чистый CSS3 без Bootstrap
- **CSS Grid & Flexbox**: Современные методы layout
- **CSS Custom Properties**: Переменные для темизации
- **Gradients**: Анимации градиентов на интерактивных элементах
- **Mobile-first**: Резиновый дизайн с мобильных устройств

### JavaScript
- **Vanilla JS**: Без jQuery, современный ES6+
- **Модульная структура**: Компонентная архитектура
- **AJAX**: Fetch API с обработкой ошибок
- **Event Delegation**: Эффективная обработка событий
- **Debounce/Throttle**: Оптимизация производительности

### HTML Templates
- **Django Template Language**: Полная интеграция
- **Template Inheritance**: DRY принцип
- **CSRF Protection**: Встроенная защита
- **Responsive Images**: Адаптивные изображения
- **ARIA Attributes**: Доступность

## 🚀 Компоненты UI

### 1. Навигация (Navbar & Sidebar)
- Фиксированная navbar (sticky)
- Коллапсируемый sidebar
- Role-based меню
- Мобильная адаптация
- Breadcrumbs

### 2. Кнопки
- **Primary**: Градиент, синяя, основные действия
- **Secondary**: С контуром
- **Danger**: Красная, для удаления
- **Sizes**: Small (32px), Default (40px), Large (48px)
- **States**: normal, hover, active, disabled, loading

### 3. Формы
- Все типы полей: text, email, password, number, select, textarea
- Валидация в реальном времени
- Показ/скрытие пароля
- Чекбоксы и радиокнопки (кастомные)
- Переключатели (switches)
- Хелп-текст и ошибки
- Required поля с asterisk

### 4. Таблицы
- **Сортировка**: По клику на заголовок (▲▼)
- **Фильтрация**: Поиск и фильтры по столбцам
- **Пагинация**: Постраничная навигация
- **Actions**: Edit, Delete кнопки
- **Responsive**: Карточный вид на мобильных
- **Export**: Функция экспорта в CSV

### 5. Карточки (Cards)
- Белый фон с тенью
- Заголовок, контент, footer
- Статистические карточки с числами
- Hover эффекты

### 6. Модальные окна
- Overlay (полупрозрачный фон)
- Центрированный контент
- Кнопки OK/Cancel
- Закрытие по клику на фон или Esc
- AJAX загрузка контента
- Разные размеры

### 7. Алерты
- **Success**: Зеленая граница + иконка
- **Error**: Красная граница + иконка
- **Warning**: Оранжевая граница + иконка
- **Info**: Синяя граница + иконка
- Auto-dismiss через 5 секунд

### 8. Transfer Box 🔥
- Два списка: доступные / выбранные
- Перемещение элементов (>, <, >>, <<)
- Поиск в обоих списках
- Сортировка (↑, ↓)
- Валидация дубликатов
- Проверка ограничений (min/max)

## 📱 Адаптивность

### Брейкпоинты
- **Desktop**: 1200px+ — полная ширина
- **Tablet**: 768px-1199px — адаптивная сетка
- **Mobile**: <768px — одна колонка

### Mobile Features
- Свернутый sidebar (гамбургер меню)
- Bottom navigation
- Упрощенные таблицы (карточки)
- Крупные кнопки
- Оптимизированные формы

## 🔧 Использование

### Подключение CSS
```html
{% load static %}
<link rel="stylesheet" href="{% static 'css/colors.css' %}">
<link rel="stylesheet" href="{% static 'css/main.css' %}">
<!-- и остальные CSS файлы -->
```

### Подключение JavaScript
```html
<script src="{% static 'js/main.js' %}"></script>
<script src="{% static 'js/utils.js' %}"></script>
<!-- и остальные JS файлы -->
```

### Использование Transfer Box
```html
<div class="transfer-box" id="students-transfer">
  <div class="transfer-box-list transfer-box-source">
    <div class="transfer-box-item" data-id="1" data-item='{"id": 1, "name": "Иван"}'>
      Иван Иванов
    </div>
  </div>
  
  <div class="transfer-box-controls">
    <button class="btn btn-primary move-right">></button>
    <button class="btn btn-primary move-left"><</button>
  </div>
  
  <div class="transfer-box-list transfer-box-target"></div>
</div>
```

### Валидация форм
```html
<form method="post" data-validate>
  <input type="email" required data-validate="email">
</form>
```

### AJAX запросы
```javascript
SchoolApp.ajax({
  url: '/api/endpoint/',
  method: 'POST',
  data: formData,
  success: function(response) {
    console.log('Success:', response);
  },
  error: function(error) {
    console.error('Error:', error);
  }
});
```

## 🎨 Кастомизация

### Изменение цветов
Редактируйте `colors.css`, изменяйте CSS переменные:
```css
:root {
  --color-primary: #2563eb;
  --color-secondary: #1e40af;
  /* и другие переменные */
}
```

### Добавление новых компонентов
1. Создайте CSS файл в `static/css/`
2. Подключите в шаблоне после других CSS
3. Создайте JS файл в `static/js/`
4. Добавьте в `SchoolApp.components`

### Расширение валидации
```javascript
SchoolApp.components.Forms.addValidator('studentId', function(value) {
  if (!/^\d{4,8}$/.test(value)) {
    return 'ID должен содержать 4-8 цифр';
  }
  return true;
});
```

## 📦 Зависимости

### Внешние
- **Google Fonts**: Inter, Poppins
- **Font Awesome**: Иконки (опционально)

### Внутренние
- Django 3.2+
- Python 3.8+
- Современный браузер (ES6+ support)

## 🌟 Особенности

### Производительность
- Минимальное количество запросов
- Оптимизированные селекторы
- Lazy loading изображений
- Debounce на вводе

### Доступность (A11y)
- ARIA атрибуты
- Keyboard navigation
- Screen reader support
- High contrast mode ready

### Безопасность
- CSRF protection
- XSS prevention
- Input sanitization
- Secure cookies

### SEO
- Semantic HTML
- Meta tags
- Open Graph
- Structured data ready

## 🛠 Разработка

### Статическая структура
```bash
# Создать структуру
mkdir -p static/css static/js templates

# Запустить development server
python manage.py runserver
```

### Тестирование
- Все компоненты протестированы на Chrome, Firefox, Safari, Edge
- Mobile: iOS Safari, Chrome Mobile
- Responsive: все разрешения от 320px до 4K

## 📄 Лицензия

MIT License - свободное использование и модификация.

## 🤝 Участие

Приветствуются issue и pull request для улучшения системы.

---

**Статус**: ✅ Готово к использованию
**Версия**: 1.0.0
**Дата**: 2024
